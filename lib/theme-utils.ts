// lib/theme-utils.ts
// lib/theme-utils.ts

// Removed getActiveTheme function as it's no longer needed after removing themes from theme-config.ts
// If other theme-related utility functions existed here, they would remain if still used.

// Example Usage (can be removed or kept for testing):
// const today = new Date();
// const currentCountry = 'in'; // Example
// const activeTheme = getActiveTheme(currentCountry); // This function no longer exists
// console.log(`Active theme for ${currentCountry} on ${today.toDateString()}:`, activeTheme?.name || 'None');
